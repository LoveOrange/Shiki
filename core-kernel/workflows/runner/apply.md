# Runner Apply

Compatibility alias for Prompt manual `next`.

Call `shiki task next [item]`, execute exactly the returned Context Envelope in the current Coding Agent session, call `shiki task complete ...`, then return control to the developer. Do not batch items or trigger Review automatically.
