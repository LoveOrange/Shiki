# Providers

This module owns execution backend adapters.

Provider support is intentionally empty for now. Future adapters for DevAgent, CLI coding agents, LLM SDKs, or subagents should normalize execution results back to Core Kernel evidence.
